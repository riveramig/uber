package Environment;

public enum EnvironmentMessageType {
    GET_PATH_COST,
    WHERE_IS_USER,
    WHERE_IS_CAR,
    UPDATE_CAR_POSITION,
    UPDATE_USER_POSITION,
    GET_USERS_IN_NEIGHBOR,
    GET_VEHICLES_IN_NEIGHBOR
}
